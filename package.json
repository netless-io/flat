{
  "name": "root",
  "private": true,
  "scripts": {
    "preinstall": "node ./scripts/preinstall",
    "bootstrap": "yarn install --frozen-lockfile && node ./scripts/build-packages",
    "lint": "yarn workspaces run lint",
    "storybook": "yarn workspace flat-components storybook",
    "start": "node scripts/start.js",
    "start:mac": "osascript ./scripts/start.scpt",
    "start:win": "scripts\\start.cmd",
    "ship": "yarn workspace renderer-app build && yarn workspace main-app ship",
    "ship:mac": "yarn workspace renderer-app build && yarn workspace main-app ship:mac",
    "ship:win": "yarn workspace renderer-app build && yarn workspace main-app ship:win",
    "ship:all": "yarn workspace renderer-app build && yarn workspace main-app ship:win && yarn workspace main-app pack:mac"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "desktop/*",
      "web/*"
    ]
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-angular": "^11.0.0",
    "husky": "^5.0.9",
    "shelljs": "^0.8.4"
  }
}
