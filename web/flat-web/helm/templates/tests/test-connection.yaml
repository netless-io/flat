apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flat-web.fullname" . }}-test-connection"
  labels:
    {{- include "flat-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flat-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
