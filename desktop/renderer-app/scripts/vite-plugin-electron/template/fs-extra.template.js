// We will use hack method to bundle file and export it with ESM
const {
    copy,
    ensureDir,
    remove,
    pathExists ,
} = require('fs-extra');
export {
    copy,
    ensureDir,
    remove,
    pathExists,
}
