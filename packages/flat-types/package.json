{
  "name": "flat-types",
  "version": "0.1.0",
  "description": "Shared TypeScript typings for the Flat app and Flat Server.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "tsc",
    "build": "tsc",
    "postinstall": "yarn run build",
    "lint": "lint-staged"
  },
  "devDependencies": {
    "lint-staged": "^10.5.4",
    "prettier": "^2.2.1",
    "typescript": "^4.0.5"
  }
}
